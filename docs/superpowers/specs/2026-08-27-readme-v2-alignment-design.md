# README 2.0.0 对齐设计

> 归档状态：这是一次性历史设计/执行记录，不是当前路线图、待办列表或操作手册。未勾选项不表示仍未实现；当前事实请查阅 [文档导航](../../README.md)、[开发状态](../../DEVELOPMENT_PLAN.md)、[当前架构](../../architecture.md) 和实际源码/测试。


## 目标

将仓库根目录 README 从混合内部实现细节与旧版功能描述的状态，重写为面向华为应用市场 2.0.0 正式版用户的项目说明，并以当前 `main` 代码为唯一功能依据。

## 范围

- 修改 `README.md`，明确当前华为应用市场正式版为 2.0.0。
- 更正首页月历、TTS、颜色主题、统计数量和 ArkUI/Web 组件等过时或不准确表述。
- 补充 2.0.0 已实现但 README 遗漏的桌面服务卡片、学习提醒、简单/实验模式、牌组整理等能力。
- 保留仍然有效的截图、免责声明、技术栈、构建和许可证信息。
- 不修改应用代码、版本号、资源、发布包或其他项目说明文件。

## 信息依据

1. `AppScope/app.json5` 的 `versionCode` / `versionName`。
2. 当前 `main` 中实际被页面引用的 ArkTS 组件与相关契约测试。
3. `UPSTREAM.lock`、`rust-toolchain.toml`、`package.json` 等构建事实。
4. 用户确认的华为应用市场当前正式版本号 2.0.0。

## README 结构

1. 项目定位、正式版本与安装入口。
2. 应用截图。
3. 2.0.0 功能概览：学习与调度、首页与统计、浏览与编辑、个性化、数据与同步、HarmonyOS 能力。
4. 实现说明：ArkUI 原生界面，Anki HTML 卡片通过 Web 组件渲染。
5. 技术栈、构建要求、Anki 源码与构建命令。
6. 项目关系与许可证。

## 文案约束

- 只描述用户当前能访问或项目当前真实提供的能力。
- 不把未接入界面的残留组件、资源字符串或内部缓存策略写成用户功能。
- TTS 只表述为对 Anki `[anki:tts]` 模板指令的自动朗读与学习页控制。
- 首页表述为 8 页学习摘要；年历热力图仅归入统计页，不再宣称首页有“热力图月历”。
- 颜色主题表述为 7 套配色，支持浅色/深色，不再写“6 种”或 HSV 派生。
- 统计表述为 13 个统计区块，其中稳定度、可提取性仅在 FSRS 下显示。

## 验证

- 搜索 README，确保旧文案“6 种颜色主题”“热力图月历”“11 张图表”“非 WebView”“音色人物/语音版本号缓存”均已删除。
- 对照版本、主题列表、首页分页、统计区块、TTS 调用、桌面服务卡片与提醒入口的源码引用。
- 运行 `npm test`，确认文档改动未破坏项目契约测试。
- 检查 `git diff --check` 与最终 diff，确保无空白错误和无意外文件修改。
